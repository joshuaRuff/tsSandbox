let myString: string;
let myNum: number;
let myBool: boolean;
let myVar: any;

let strArray: string[]; // || Array<string>
let numArray: number[]; // || Array<number>
let boolArray: boolean[]; // || Array<boolean>

let strNumTuple: [string, number];

myString = 'Hello ' + 'Word';
myNum = 22;
myBool = true;
myVar = 5 + '5';

strArray = ['Hello', 'World'];
numArray = [1, 2, 3];
boolArray = [true, false, true];

strNumTuple = ['Hello', 4 , 5];

let myVoid: void = null // || undefined;

let myUndefined: undefined = undefined;
let myNull: null = null 
